import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: any) => ({
  title: {
    fontSize: "2.5rem",
    fontWeight: 700,
    marginBottom: "1rem",
    color: "#262626",
  },
  text: {
    fontSize: "1.2rem",
    lineHeight: "1.5rem",
    marginBottom: "1rem",
    alignText: "justify",
    color: "#262626",
  },
}));

export default useStyles;
